import Foundation
import QuartzCore
/**
 * - Note: So this class represents the view you can replicate and add your own logic too
 */
class CustomView: HorView {
   override init(frame: CGRect = .zero) {
      super.init(frame: frame)
   }
}
